# ~/.profile: executed by Bourne-compatible login shells.

#/opt/Nextcloud-2.3.3-x86_64.AppImage&

export $TERM=xterm-256color
if [ "$BASH" ]; then
  if [ -f ~/.bashrc ]; then
    . ~/.bashrc
  fi
fi

#mesg n
